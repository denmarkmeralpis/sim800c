# frozen_string_literal: true

RSpec.describe Sim800c do
  it "has a version number" do
    expect(Sim800c::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
